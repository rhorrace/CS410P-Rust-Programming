pub fn build_proverb(list: Vec<&str>) -> String {
    if list.is_empty() {
			String::new()
		}
		let proverb: String = String::new();
		let all: String  = "And all for the want of a".to_string();
		let want: String = "For want of a".to_string();
		let lost: String = "was lost.".to_string();
		let first = &list.to_string();
		if list.len == 1 {
			
		}
		proverb
}
